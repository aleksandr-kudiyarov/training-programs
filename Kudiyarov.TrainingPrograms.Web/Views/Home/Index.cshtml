@model IEnumerable<Kudiyarov.TrainingPrograms.Programs.TrainingProgram>

@{
    ViewBag.Title = "title";
}

<ul>
    @foreach (var program in Model)
    {
        <li>@Html.ActionLink(program.Name, "Program", new { type = (int)program.Type })</li>
    }    
</ul>
